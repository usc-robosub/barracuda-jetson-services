# barracuda-static-ip.conf - Configuration for Static IP Service

# Network Interface
INTERFACE=eth0

# Static IP Address (with subnet mask)
IP_ADDRESS=192.168.1.1/24

# DNS Servers (separated by spaces)
DNS_SERVERS="192.168.1.1"

# DNS Domain
DOMAIN="barracuda.local"
